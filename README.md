# Blind Box Pastel

Public blind box page. Replace `YOUR_WEB_APP_URL` in `index.html` with your Google Apps Script Web App URL to enable saving results to Google Sheets.

Open `index.html` in a browser to preview locally.
